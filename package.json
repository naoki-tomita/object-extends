{
  "name": "object-extention",
  "version": "1.0.4",
  "main": "dist/index.js",
  "license": "MIT",
  "types": "dist/index.d.ts",
  "devDependencies": {
    "jest": "^26.4.0",
    "ts-jest": "^26.2.0",
    "ts-node": "^8.10.2",
    "typescript": "^3.9.7"
  },
  "jest": {
    "preset": "ts-jest"
  },
  "scripts": {
    "prebuild": "rm -rf ./dist",
    "build": "tsc -p tsconfig.build.json",
    "prepublish": "yarn build",
    "test": "jest"
  }
}
